'use strict';

/**
 * max-delivery-charge service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::max-delivery-charge.max-delivery-charge');
