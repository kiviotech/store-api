'use strict';

/**
 * max-delivery-charge router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::max-delivery-charge.max-delivery-charge');
